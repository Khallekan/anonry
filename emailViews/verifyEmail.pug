extends baseEmail

block content
  p Hi #{user_name},
  h2 Anonry Email Verification
  p #{subject}
  p #{message}
  h4 #{otp}
  strong 
    a(href=link) #{link}
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            
  p - Anonry
